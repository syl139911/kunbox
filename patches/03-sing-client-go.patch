--- a/protocol/http/client.go
+++ b/protocol/http/client.go
@@ -22,6 +22,7 @@ type Client struct {
 	host       string
 	path       string
 	headers    http.Header
+	delHost    bool
 }
 
 type Options struct {
@@ -31,6 +32,7 @@ type Options struct {
 	Path     string
 	Headers  http.Header
+	DelHost  bool
 }
 
 func NewClient(options Options) *Client {
@@ -41,6 +43,7 @@ func NewClient(options Options) *Client {
 		password:   options.Password,
 		path:       options.Path,
 		headers:    options.Headers,
+		delHost:    options.DelHost,
 	}
 	if options.Dialer == nil {
 		client.dialer = N.SystemDialer
@@ -62,13 +65,23 @@ func (c *Client) DialContext(ctx context.Context, network string, destination M.
 	}
 	defer conn.Close()
 
+	// Build target: destination [+ path injection]
+	target := destination.String()
+	if c.path != "" {
+		target = target + c.path
+	}
+
 	request := &http.Request{
 		Method: http.MethodConnect,
+		URL: &url.URL{
+			Host: target,
+		},
 		Header: http.Header{
 			"Proxy-Connection": []string{"Keep-Alive"},
 		},
 	}
-	if c.host != "" && c.host != destination.Fqdn {
+
+	if c.delHost {
+		request.URL = &url.URL{Opaque: target}
+		request.Host = ""
+	} else if c.host != "" && c.host != destination.Fqdn {
 		if c.path != "" {
 			_ = conn.Close()
 			return nil, E.New("Host header and path are not allowed at the same time")
